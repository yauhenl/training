plugins {
    id 'java'
    id 'application'
    id 'org.gradle.java.experimental-jigsaw' version '0.1.1'
}

import java.util.regex.Matcher;

dependencies {
    implementation project(':api')

    runtimeOnly project(':newyear')
    runtimeOnly project(':easter')
}

javaModule.name = 'com.yauhenl.training.main'

mainClassName = 'com.yauhenl.training.main.Congratulator'